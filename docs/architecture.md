# Architecture

- `src/petstore_mcp`: MCP server, tools, resources, client, and schemas.
- `tests/petstore_mcp`: mirrored tests for each module.
- `.github/workflows`: CI, release, and docs workflows.
